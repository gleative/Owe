import React, { Component } from 'react'

export default class YouOwe extends Component {
  render() {
    return this.props.todos.map(
      
    )
  }
}

const containerStyle = {
    height: 'auto', // So we wont have to worry about height depending on size of list
}
